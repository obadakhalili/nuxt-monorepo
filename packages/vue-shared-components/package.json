{
  "name": "@nuxt-monorepo/vue-shared-components",
  "version": "1.0.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vue-cli-service serve dev/serve.js",
    "build:Banner": "vue-cli-service build --target lib --name Banner --formats commonjs components/Banner.vue --dest dist/banner",
    "build:Header": "vue-cli-service build --target lib --name Header --formats commonjs components/Header.vue --dest dist/header"
  },
  "devDependencies": {
    "@vue/cli-service": "^4.5.12"
  }
}
